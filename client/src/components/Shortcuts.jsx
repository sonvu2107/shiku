/**
 * Shortcuts component removed — replaced with a harmless placeholder.
 * This module used to render the shortcuts sidebar; the feature was removed.
 * Keeping a placeholder prevents import errors in case some files still import it.
 */
export default null;
